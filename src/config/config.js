/**
 * @author: Valentin Cadot
 * @version: 1.0.0
 */

/**
 * General game configuration
 */
export const gameConfig = {
    gameSpeed: 3,
    gameScale: 1,
}

export const gameDebug = {
    flagDebug: false,
    debugLevel: 2,
}

export const backgroundConfig = {
    nbBackgroundStars: 30,
    nbForegroundStars: 10,
    nbBackgroundFogs: 10,
    nbForegroundFogs: 5,
}

// TODO add player config

export const enemyConfig = {
    enemySpeedSpawn: 90,
}

export const shieldPackConfig = {
    shieldPackSpeedSpawn: 1000,
}

// module.exports = module.default = [ gameConfig, debugConfig ];